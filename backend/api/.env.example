# Backend API environment variables
DATABASE_URL=postgresql://user:password@localhost:5432/samis_guide
PORT=4000

# Supabase Auth (production)
# SUPABASE_URL=https://<project-ref>.supabase.co

# Dev-only: set to "true" to skip JWT verification (Bearer token = raw user UUID)
DEV_MOCK_AUTH=true
